# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=library_app_development

# Rails Configuration
# Generate a new secret key with: docker-compose run --rm web bundle exec rails secret
SECRET_KEY_BASE=your_secret_key_here_replace_with_generated_key
